@{
    ViewData["Title"] = "Disable two-factor authentication (2FA)";
    ViewData.AddActivePage(ManageNavPages.TwoFactorAuthentication);
}

<h2>@ViewData["Title"]</h2>

<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Ta akcja wyłącza tylko uwierzytelnianie dwuskładnikowe.</strong>
    </p>
    <p>
        Wyłączenie uwierzytelniania dwuskładnikowego nie zmienia kluczy używanych w aplikacjach uwierzytelniających. Jeśli chcesz zmienić klucz
        użyty w aplikacji uwieżytelniającej powinieneś <a asp-action="ResetAuthenticatorWarning">
            zresetować swoje
            klucze uwieżytelniające.
        </a>
    </p>
</div>

<div>
    <form asp-action="Disable2fa" method="post" class="form-group">
        <button class="btn btn-danger" type="submit">Disable 2FA</button>
    </form>
</div>
