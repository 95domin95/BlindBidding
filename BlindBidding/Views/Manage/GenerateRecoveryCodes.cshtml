@{
    ViewData["Title"] = "Wygeneruj kody odzyskiwania 2-krotnego uwierzytelniania (2FA)";
    ViewData.AddActivePage(ManageNavPages.TwoFactorAuthentication);
}

<h2>@ViewData["Title"]</h2>

<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Ta akcja generuje nowe kody odzyskiwania.</strong>
    </p>
    <p>
        Jeśli zgubisz urządzenie i nie masz kodów odzyskiwania, utracisz dostęp do swojego konta.
    </p>
    <p>
        Generowanie nowych kodów odzyskiwania nie zmienia kluczy używanych w aplikacjach uwierzytelniających. Jeśli chcesz zmienić klucz
        użyte w aplikacji uwierzytelniającej <a asp-action="ResetAuthenticatorWarning">zresetuj klucze uwierzytelniające.</a>
    </p>
</div>

<div>
    <form asp-action="GenerateRecoveryCodes" method="post" class="form-group">
        <button class="btn btn-danger" type="submit">Wygeneruj kody odzyskiwania</button>
    </form>
</div>
